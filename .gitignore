_source/
input/
temp/
output/
.claude/settings.local.json
app/node_modules/
app/.env

# Dados de cliente / artefatos pesados — nunca versionar (LGPD)
*.csv
guia-exemplo/
Ajsute de design/
inde-conversao-perfil*.html

# Python
__pycache__/
*.pyc

# Dados de runtime (SQLite + log das chamadas Claude) — nunca versionar
app/data/
# SQLite baixado de produção p/ refino (dado de cliente) + dump de análise — nunca versionar
*.db
*.db-wal
*.db-shm
comments-prod.db
temp_feedback_*.md

# Scratch de uploads (CSV bruto transitório) — nunca versionar
app/.scratch/
# Dado-base retido por análise (Fase 3b) — dado de cliente, nunca versionar
app/.base/
