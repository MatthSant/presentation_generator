# Texto escrito pelo consultor (autoria='consultor') cita números declarados: não passa pelo validador de tabela
Tipo: definicao

O validador protege o número do agente. O do consultor é dele; fica marcado como declarado, não como calculado.
