# Número entra como número (o builder formata) ou via bind; texto com número digitado é recusado
Tipo: regra

É o que garante que todo número do relatório nasceu no Python. A prosa cita só o que está numa tabela ou num card.
